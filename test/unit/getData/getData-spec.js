import {getData} from 'modules/getData';

describe("getRSSFeed", () => {

    it('Should return a JSON object', () => {

        // expect(getData('base/json.json')).toEqual('hello');

        // done();

    });

});
